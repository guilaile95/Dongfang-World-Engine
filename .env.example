# Copy to .env (gitignored). Never commit real keys.
# The process reads only these names. Do not put secrets in code, fixtures, or logs.

DWE_LLM_BASE_URL=http://localhost:10100/v1
DWE_LLM_API_KEY=
DWE_LLM_MODEL=
DWE_WORLD_FILE=data/local/world.sqlite
# Product play: path to a real world TXT/Markdown (not committed).
# DWE_WORLD_SOURCE=C:\Users\DINOL\Downloads\龙族V1.0.txt
# Optional transport. Unset cost rates leave cost blank in call records.
# DWE_LLM_FALLBACK_MODEL=
# DWE_LLM_MAX_RETRIES=2
# DWE_LLM_TIMEOUT_MS=60000
# DWE_LLM_INPUT_USD_PER_MTOK=
# DWE_LLM_OUTPUT_USD_PER_MTOK=
